import pygame 
import sys

pygame.init();
class food():
    def __init__(self, screen,size = [20, 20], color = (128,0,0), pos = [15, 15]):
        self.screen = screen
        self.width = width
        self.length = length
        self.color = color
        self.posx = posx
        self.posy = posy

   def drawfood(self, screen, color, pos):
        pygame.draw.rect(screen, RED,())

    
